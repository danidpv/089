# 089 Branding

`logo089-source.pdf` is the source of truth for the official 089 mark.

The web asset in `public/images/branding/logo-089.svg` was extracted from that PDF as vector paths, cropped to the artwork bounds, and kept without a background. Do not rebuild the mark by typing `089` with Syne or any other font.
